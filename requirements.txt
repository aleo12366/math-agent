# Math Agent - Competition Dependencies
# Only includes packages needed by the pipeline (not the web server)
pydantic>=2.10.0
pydantic-settings>=2.7.0
aiohttp>=3.11.0
sympy>=1.12.0
scipy>=1.11.0
numpy>=1.24.0
